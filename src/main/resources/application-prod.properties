# JPA Settings
#spring.jpa.show-sql: true
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:postgresql://team-2-p2.cvtq9j4axrge.us-east-1.rds.amazonaws.com:5432/postgres?currentSchema=team2-project2
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name =com.postgres.jdbc.Driver

# Configure Port and Context Path
server.port=5000
server.servlet.context-path=/api


# In the master application.properties file we can control which Spring Profile we want to use
spring.profiles.active=dev

# will find by id without error (JK it was because we were using getById instead of findById)
#spring.jackson.serialization.fail-on-empty-beans=false
